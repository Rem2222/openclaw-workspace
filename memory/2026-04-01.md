# Daily Notes — 2026-04-01

## Dashboard Этап 9 — Редизайн (завершён)

**Выполнено subagent-ами на GLM-5:**
- Task 1 (workspace-bzd) ✅ Layout + Sidebar
- Task 2 (workspace-duc) ✅ Agents + Sessions + Subagents
- Task 3 (workspace-lp9) ✅ Tasks + Cron + ActivityFeed
- Task 4 (workspace-11o) ✅ Approvals + StatusIndicator

**Beads integration:**
- `bd:workspace-xxx` label для subagent sessions
- Subagent делает `bd update <id> --claim` в начале и `--status done` в конце
- Привязка сессий к задачам: `POST /api/issues/sessions {sessionKey, issueId}`

**Фичи Dashboard:**
- Страница **Проекты** (Issues → Проекты, route `/projects`)
- Страница **Монитор** (`/monitor`) — Activity, Messages, Tasks, Docs
- **Projects → Monitor**: кнопка 📊 на раскрытом ряду, фильтрация по `?project=`
- **Settings** — GatewaySelector вынесен из footer
- CSS от nekocode.app (Catppuccin Mocha/Latte, JetBrains Mono)

**API endpoints:**
- `GET /api/issues` — список задач из Beads
- `GET /api/issues/:id` — одна задача
- `POST /api/issues/sessions` — привязать сессию к задаче
- `GET /api/issues/:id/sessions` — сессии задачи
- `GET /api/issues/session-task-map` — весь маппинг
- `GET /api/sessions` — читает напрямую из файла сессий (25 vs 1 через Gateway API)
- `POST /api/spawn` — spawn сессии (backend не умеет, нужен agent tool)

**Проблемы:**
- Backend `/api/spawn` не работает — `sessions_spawn` это agent tool, не Gateway tool
- Cloudflare tunnel давал 404, Pinggy работает стабильно
- Express route order: `/session-task-map` должен быть ДО `/:id` иначе перехватывается

**Git commits:**
- `c04fa8c` Dashboard backup before Этап 9
- `9beaf7d` Superpowers: GLM-5 as default agent
- `f1f70dd` Dashboard: mobile responsive + Issues + memory
- `379c1c7` Dashboard: Projects page updates (new project, archive, session binding)
- `ea8a86c` Dashboard: Monitor page
- `718c091` Sessions API reads from file directly
- `0e0c462` Dashboard: Projects → Monitor navigation, session-task-map fixed

**Tunnel (Pinggy):** `https://pqsmq-185-207-139-4.a.free.pinggy.link`

---

## Сервера (15:37)

- 🟢 Backend, Frontend, OpenMOSS
- 🔴 Ollama, MissionControl

---

## Campfire Survival (на паузе)

- Освещение не работает — игрок видит синий оттенок, костра не видно
- `GameScene.js` в процессе исправления
- Dev server: `cd ~/.openclaw/workspace/projects/Campfire-Survival && npm run dev`

---

## Superpowers SKILL.md обновления

- **Beads обязателен** (был optional)
- **GLM-5 как дефолтный агент** (opencode-go/glm-5)
- При запуске проекта спрашивать какой агент использовать
- Subagent label формат: `bd:<ISSUE_ID>`

---

## Модель и токены

- Модель: `minimax/m2.7` (minimax/minimax-m2.7 via OpenRouter)
- Контекст на ~15:37 — ~44k/200k (22%), свободно ~156k токенов
