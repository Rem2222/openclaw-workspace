# Daily Notes — 2026-04-02

## Session: Dashboard Этап 9 завершён

### Что сделано

**Dashboard Redesign (Этап 9):**
- 4 subagent-таска завершены на GLM-5:
  - Task 1 (workspace-bzd): Layout + Sidebar
  - Task 2 (workspace-duc): Agents + Sessions + Subagents
  - Task 3 (workspace-lp9): Tasks + Cron + ActivityFeed
  - Task 4 (workspace-11o): Approvals + StatusIndicator
- Стили nekocode-landing.css скопированы в `src/styles/nekocode-landing.css` (Catppuccin Mocha/Latte, JetBrains Mono)
- Все страницы перестилизованы под новый CSS

**Monitor page (новая функциональность):**
- Projects → Sessions/Subagents → Monitor навигация с highlight
- Issues → Проекты переименование
- Сессия/Агент кнопка (highlight + scroll к строке)
- Сворачиваемые секции (▶/▼) для Tasks, Subagents, Activity
- handleSendMessage → real API (`POST /api/chat/send` → JSONL)
- maxHeight чата увеличен до 300px
- placeholder/minHeight для стабильного layout

**API routes созданы:**
- `GET /api/issues` — список задач из Beads
- `GET /api/issues/session-task-map` — маппинг сессий к задачам
- `POST /api/issues/sessions` — регистрация сессии за задачей (для Superpowers)
- `POST /api/chat/send` — отправка сообщения в сессию
- `GET /api/activity` — глобальная лента активности

**Superpowers workflow:**
- Subagents получают label `bd:<ISSUE_ID>`
- При спавне: `bd update <id> --claim`
- При завершении: `bd update <id> --status done`
- Добавлен `-d` флаг для описания задачи

**Beads tasks:** Все 34 задачи закрыты (включая 6 новых для Монитора)

### Коммиты
- 41a6758 — End of day backup
- f8bb03f — maxHeight чата → 300px
- b9bf810 — placeholder, minHeight, handleSendMessage API
- cbb2879 — сворачиваемые секции в Мониторе
- 5dd7820 — бэкап перед задачами
- e5231fd — фикс счётчика open issues

### Решённые проблемы
1. sessionTaskMap direction bug — исправлен (map is sessionKey→issueId, not reverse)
2. Projects.jsx corrupted by subagent — восстановлен из git
3. Results API 500 error — route ordering conflict (issues.js vs results.js)
4. Monitor Activity исчезал — filterSessions race condition
5. Collapsible sections добавлены в Tasks, Subagents, Activity

### Открытые вопросы (закрыты notes)
- workspace-7p5 (P2): Tab switching slow — требует профилирования в DevTools
- workspace-41i: Пагинация чата — backend не поддерживает offset/limit
- workspace-9hz: Полная интеграция чата с sessions_send — требует отдельного агента-посредника

### Сессия
- Время: 03:17 MSK (ночная)
- Токены: 85k in / 394 out
- Контекст: 113k/200k (56%)

---

## Session: Dashboard_Gwen36 — Рефакторинг и багфикс (вечер)

### Что сделано

**Полный аудит кода (~30 файлов):**
- Backend: 14 route files, gateway.js, index.js, websocket.js
- Frontend: 11 page components, 2 contexts, Layout, utility components

**Найдено 10 багов, все исправлены через 7 субагентов (zai/glm-5.1):**

P0 (главная причина тормозов):
- CountsContext: 9 fetch каждые 5с → 8 fetch каждые 30с
- Каждый компонент дублировал polling 5с → 15с
- Нагрузка снижена в 10-15x

P1:
- Console.log спам (10 frontend + 11 backend файлов)
- GatewayClient instantiated 4x → singleton
- Command injection в /api/command (CWE-78) → allowlist
- Kill session вызывал несуществующий route
- Route order /session-task-map

P2:
- Monitor.jsx: 23 IIFE → useMemo
- Projects.jsx: loadSessionTaskMap dedup → useCallback
- Session/:id path traversal protection
- StatusBar Tailwind (ok — Tailwind настроен)

**Beads:** Все 10 задач закрыты (workspace-i30, 12p, r4j, fcr, j5s, pfw, gv3, 1w1, k47, 952)

**Субагенты:** 7 запущено, 5 завершились успешно, 2 таймаутнулись (доделал вручную + перезапуск)

**Проектные файлы обновлены:** PROJECT_STATUS.md создан
